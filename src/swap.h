#ifndef __SWAP_H__
#define __SWAP_H__

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void swap_strings(char **str1_ptr, char **str2_ptr);
void swap_strings_deprecated(char *str1, char *str2);

#endif
